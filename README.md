# Hidden URL Gateway

**KO** Event-branded gateway that shows a destination page only to allowlisted emails. The real URL never leaves the server.

**EN** Event-branded gateway that shows a destination page only to allowlisted emails. The real URL never leaves the server.

See the repository README in the working tree for the full Korean/English docs.
